enum LoadState {
  loading,
  waiting,
}

enum ParkingLotUserState {
  display,
  add,
  cancel,
  list,
  replace,
  modification,
}

enum LoginState {
  loggedOut,
  emailAdress,
  register,
  password,
  welcom,
  sendEmail,
  loggedIn,
  passReset
}

enum AuthState {
  loggedIn,
  loggedOut,
}
